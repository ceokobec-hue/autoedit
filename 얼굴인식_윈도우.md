# 윈도우에서 「얼굴 인식」 쓰기

이 도구는 **화면 어디에 사람이 있는지**를 재서 자막·카드를 사람 안 가리는 자리에 놓습니다.
맥은 애플이 운영체제에 넣어 준 기능(Vision)을 씁니다. 윈도우에는 그게 없어서 **OpenCV** 로 갑니다.

> **한 줄 요약** — 얼굴·사람 찾기는 맥과 거의 같은 자리를 잡습니다.
> 다만 **화면에 박힌 글자를 «읽지»는 못합니다** — 「글자처럼 생긴 줄」을 어림잡을 뿐입니다.
>
> ⚠️ **좌표가 얼마나 맞는지는 「맥에서 OpenCV 통로를 강제로 켜서」 잰 값입니다.**
> 두 운영체제가 같은 코드를 쓰지만, 재 본 것은 맥입니다.
> 자리가 이상하면 [Issues](https://github.com/ceokobec-hue/autoedit/issues) 에 알려 주세요 — 그게 제일 큰 도움이 됩니다.

---

## 1. 준비 — 한 줄

```powershell
pip install opencv-python
```

이게 전부입니다. 얼굴 찾기 모델은 **저장소 `models/` 폴더에 이미 들어 있습니다**(내려받을 것 없음).

- 맥 쓰시는 분은 **깔 필요 없습니다.** 안 깔아도 지금까지처럼 똑같이 돕니다.
- 잘 깔렸는지 확인:

```powershell
python -c "import cv2; print(cv2.__version__)"     # 4.7 이상이면 됩니다
```

---

## 2. 지금 어느 통로로 도는지 보기

```powershell
python auto-insert\scripts\mac\detector.py 프레임.png
```

첫 줄에 안내가 뜹니다.

```
※ 얼굴·사람 찾기: OpenCV(YuNet) 통로로 돕니다 — 이 컴퓨터에 애플 Vision 이 없습니다.
  ⚠️ 이 통로는 화면의 글자를 «읽지» 못합니다. …
```

맥에서 일부러 OpenCV 쪽을 시험해 보고 싶으면:

```bash
AUTOEDIT_DETECTOR=opencv python3 auto-insert/scripts/mac/place_captions.py --video v.mp4 --srt v.srt --out 작업폴더
```

| 환경변수 | 무엇 |
|---|---|
| `AUTOEDIT_DETECTOR` | `swift` 또는 `opencv` — 통로를 직접 고릅니다. 안 주면 알아서 고릅니다 |
| `AUTOEDIT_TEXT_DETECT=off` | 글자 어림잡기를 끕니다(부정확할 바에 「못 찾았다」로 두고 싶을 때) |
| `AUTOEDIT_YUNET_MODEL` | 모델 파일을 다른 곳에 두셨을 때 |

---

## 3. 윈도우에서 «되는 것 / 안 되는 것»

| 기능 | 윈도우 | 설명 |
|---|---|---|
| **얼굴 찾기** | ✅ 거의 같음 | 실측 9프레임에서 얼굴 중심이 맥과 가로 ±14px 안(2560px 화면 기준). [표는 4장](#4-맥과-얼마나-다른가--실제-숫자) |
| **사람 위치** | ⚠️ **어림잡기** | 진짜 「사람」을 찾는 게 아니라 **얼굴에서 몸을 계산**합니다. 폭은 맥과 비슷하지만 **좌우 위치가 어긋날 수 있습니다** |
| **구워진 자막 띠 찾기** | ⚠️ 대체로 맞음 | 실측 10프레임에서 맥과 −25 ~ +9px. 다만 **글자를 읽는 게 아니라 «글자처럼 생긴 줄»** 입니다 |
| **화면 위쪽 슬라이드 글자 범위** | ⛔ **약함** | 작고 흐린 글자(구석의 채널 이름 등)를 **놓칩니다.** 맥이 78~2471px 로 잡은 것을 1771~2467px 로 잡은 프레임이 있습니다 |
| **글자 «내용» 읽기(OCR)** | ⛔ **없음** | 애플 Vision 은 「구독하고 알림 받기」라고 읽어 줍니다. OpenCV 에는 그 기능이 없습니다. 결과 JSON 에 `text` 칸이 안 나옵니다 |

### 글자 읽기(OCR)가 없어서 실제로 뭐가 달라집니까

이 도구가 「읽은 글자」를 쓰는 데는 **한 군데도 없습니다.** 쓰는 건 **글자가 있는 자리**뿐입니다
(자막 띠 윗변 = 인서트 금지선 / 위쪽 글자 범위 = 슬라이드 영역).
그래서 **기능이 통째로 빠지지는 않습니다.** 다만 자리를 어림잡는 정확도가 낮습니다.

**어느 쪽으로 틀립니까 — 이게 중요합니다.**
헛것을 글자로 잡으면 금지선이 **더 위로** 올라갑니다. 인서트를 놓을 자리가 좁아질 뿐,
**자막을 덮는 사고로는 잘 안 갑니다.** 위험한 건 반대(글자를 놓쳐서 금지선이 내려가는 것)인데,
그때는 화면에 **「⚠️ 하단 자막을 못 찾았다 — 육안 확인 필요」** 가 찍힙니다.

**믿기 어려우시면 손으로 지정하세요.** 이 방법이 제일 확실합니다.

```powershell
python auto-insert\scripts\mac\build_inserts.py --cards cards.json --place place.json --out inserts.json
```

`place.json` 형식은 `파일형식.md` 를 보세요.

---

## 4. 맥과 얼마나 다른가 — 실제 숫자

2560×1440 영상 한 편에서 프레임 10장을 **두 통로로 각각 재서** 나란히 놓은 것입니다.
(재는 법: `AUTOEDIT_DETECTOR` 를 바꿔 가며 같은 프레임에 `detector.py` 를 돌리면 됩니다.
숫자는 영상마다 다릅니다 — 여러분 영상에서 다시 재 보세요.)

### 얼굴 — 거의 같다

| 프레임 | 맥(Vision) x,y w×h | 윈도우(OpenCV) x,y w×h | 중심 차이 |
|---|---|---|---|
| 1 | 916,469 177×177 | 927,445 217×246 | 가로 +31 · 세로 +11 |
| 2 | 927,202 555×555 | 995,166 424×578 | 가로 **+3** · 세로 −24 |
| 3 | 925,217 559×559 | 984,197 443×574 | 가로 **+2** · 세로 −12 |
| 4 | 990,221 571×571 | 1042,236 459×546 | 가로 **−4** · 세로 +3 |
| 5 | 827,231 595×595 | 889,228 442×571 | 가로 −14 · 세로 −15 |
| 6 | 888,199 531×531 | 933,166 427×561 | 가로 −6 · 세로 −18 |
| 7 | 얼굴 없음 | 얼굴 없음 | — |
| 8 | 914,210 584×584 | 986,175 429×564 | 가로 −4 · 세로 −45 |
| 9 | 991,218 550×550 | 1053,213 434×547 | 가로 +4 · 세로 −7 |
| 10 | 949,219 568×568 | 1023,181 423×545 | 가로 +2 · 세로 −50 |

가로 중심은 ±14px 안(1번 제외). **자막을 어느 쪽에 둘지 판정하기에 충분합니다.**
OpenCV 상자가 세로로 길고 가로로 좁은 것은 두 모델이 「얼굴」로 치는 범위가 달라서입니다.

### 구워진 자막 띠 윗변 — 대체로 맞다

| 프레임 | 맥 | 윈도우 | 차이 |
|---|---|---|---|
| 1 | 867 | 865 | −2 |
| 2 | 1206 | 1194 | −11 |
| 3 | 1202 | 1193 | −9 |
| 4 | 1127 | 1136 | **+9** |
| 5 | 1206 | 1194 | −11 |
| 6 | 1206 | 1192 | −13 |
| 7 | 863 | 864 | +1 |
| 8 | 1202 | 1194 | −8 |
| 9 | 1202 | 1177 | −25 |
| 10 | 1206 | 1194 | −11 |

−(마이너스)는 「금지선을 더 위로 잡았다」 = 안전한 쪽입니다.
`+9` 한 번만 맥보다 아래인데, 프로그램이 금지선에 **20px 여유**를 더 두므로 덮이지 않습니다.

### 사람 좌우 끝 — 여기가 제일 다르다

| 프레임 | 맥 L~R | 윈도우 L~R | 폭 차이 | 자막 놓을 쪽 |
|---|---|---|---|---|
| 1 | 480~1521 | 537~1535 | −43 | 오른쪽 / 오른쪽 |
| 2 | 260~2370 | 231~2184 | −157 | 왼쪽 / **오른쪽** |
| 3 | 276~2377 | 187~2225 | −63 | 왼쪽 / **오른쪽** |
| 4 | 292~2426 | 215~2328 | −20 | 왼쪽 / **오른쪽** |
| 5 | 303~2365 | 93~2128 | −28 | 왼쪽 / **오른쪽** |
| 6 | 340~2232 | 164~2130 | +74 | 왼쪽 / **오른쪽** |
| 9 | 300~2067 | 272~2266 | +227 | 오른쪽 / 오른쪽 |
| 10 | 323~2048 | 261~2209 | +222 | 오른쪽 / 오른쪽 |

**폭은 잘 맞습니다.** 어긋나는 건 **좌우 위치**입니다.
맥의 사람 상자는 **팔 뻗은 쪽으로 치우칩니다.** 어느 쪽으로 치우칠지는 예측할 수 없어서
(같은 영상에서도 왼쪽 −0.20 ~ 오른쪽 +0.50 얼굴폭만큼 왔다갔다 합니다)
윈도우 쪽은 **얼굴 한가운데를 사람 한가운데로** 놓습니다.

그래서 **화자가 화면 한가운데 서 있으면 자막을 왼쪽에 둘지 오른쪽에 둘지가 갈립니다.**
위 표의 2~6번이 그런 경우인데, 사람 중심이 화면 정중앙(1280px)에서 **35~73px** 밖에 안 떨어져 있습니다.
어느 쪽도 「틀린」 답은 아니지만, **화면 한쪽에 자막·그래픽이 이미 있으면 그 위에 놓일 수 있습니다.**

> ⛔ **그래서 윈도우에서는 검수 시트를 꼭 보고 넘어가세요.**
> `review_sheet.py` · `plan_check.py` 는 원래 「사람이 보고 판정하라」고 있는 단계입니다.
> 맥에서는 넘겨도 대개 괜찮았지만, **윈도우에서는 이 단계가 진짜로 필요합니다.**

---

## 5. 잘 안 될 때

| 증상 | 원인 · 하실 일 |
|---|---|
| `⛔ opencv-python 이 없습니다` | `pip install opencv-python` |
| `⛔ opencv-python 이 너무 낮습니다` | `pip install --upgrade opencv-python` (4.7 이상 필요) |
| `⛔ 얼굴 찾기 모델 파일이 없습니다` | `models/` 폴더를 같이 가져오셨는지 보세요. `models/README.md` 참고 |
| `⛔ 얼굴 찾기 모델을 열지 못했습니다` | 파일이 **132바이트**면 내려받다 만 것입니다. `models/README.md` 의 「파일이 깨졌는지 확인하기」 |
| 얼굴을 하나도 못 찾음 | 화면에 얼굴이 정말 큰가요? 아주 큰 얼굴은 놓칠 수 있습니다. `models/` 파일 2개가 다 있는지도 보세요 |
| 한글 폴더에서만 안 됨 | 이건 막아 뒀습니다. 그래도 나면 알려 주세요(`detect_opencv.py` 의 `imread_any`) |

---

## 6. 만든 사람용 메모 — 구조

```
auto-insert/scripts/mac/
  detector.py        ← 통로를 «고르는» 곳. 부르는 쪽은 전부 여기만 부른다
  detect_opencv.py   ← OpenCV 구현 (윈도우·리눅스)
  speaker_box.swift  ← 애플 Vision 구현 (맥) · 손대지 않았다
models/
  face_detection_yunet_2023mar.onnx   ← OpenCV 4.x 용
  face_detection_yunet_2026may.onnx   ← OpenCV 5.x 용
  YuNet-LICENSE.txt                   ← MIT
```

부르는 쪽 세 군데(`place_captions.py` · `ots_place.py` · `pipeline_v2/place.py`)는
`detector.detect(프레임목록, want_text=…)` 한 줄만 부릅니다.
**맥에서는 예전과 완전히 같은 경로(swift)로 갑니다** — 결과 JSON 도 글자 하나 안 달라집니다.
