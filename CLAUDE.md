# autoedit — AI 에이전트용 규칙

이 저장소를 Claude Code 같은 AI 코딩 도구로 열었을 때 지킬 것.

## 이 저장소의 약속 (사용자에게 한 약속이다 — 깨지 말 것)

1. **아무것도 자동으로 실행하지 않는다.** 설치 스크립트·부트스트랩·백그라운드 작업을 만들지 않는다.
2. **인터넷으로 아무것도 보내지 않는다.** 예외는 `get_fonts.sh`(공개 폰트 내려받기)뿐이다.
3. **사용자가 지정하지 않은 파일을 읽거나 지우지 않는다.**

## 절대 하지 말 것

- ⛔ **원본 영상을 덮어쓰지 않는다.** 출력은 항상 새 파일로. `-y` 로 입력 파일을 향하게 두지 말 것.
- ⛔ **사람의 승인 게이트를 건너뛰지 않는다.** `build_review.py`(멀티캠)·`review_sheet.py`·
  `plan_check.py`(오토인서트)는 **사람이 보고 판정하라고** 있는 단계다. 자동으로 통과시키지 않는다.
- ⛔ **폰트 파일을 저장소에 커밋하지 않는다.** 8종의 재배포 조건이 제각각이라
  (SIL OFL 넷은 자유, 국내 기업 글꼴 넷은 라이선스 전문 동봉 조건) 한 저장소에서 다 지키기 어렵다.
  `get_fonts.sh` 가 각자 공식 배포처에서 받게 한다. 근거는 `THIRD-PARTY-NOTICES.md`.
- ⛔ **영상·음성·자막 파일을 커밋하지 않는다.** 사람 얼굴과 목소리가 들어 있다.
- ⛔ **남의 것과 「내 회차 내용」을 넣지 않는다.** 고객사·기관명·수강생 정보는 물론이고,
  실제 강의 대본·인서트 문구·회차 목차도 코드 기본값으로 박지 않는다.
  화면에 나올 글자는 전부 «예시»로 두고, **쓰는 사람이 자기 것으로 채우게 한다.**
  (만든 사람 표시는 예외 — `LICENSE`·`README` 의 저작권·크레딧은 그대로 둔다.)
- ⛔ **개인 경로를 코드에 박지 않는다.** `/Users/누구/...` 같은 절대경로 금지 —
  CLI 인자나 환경변수(`FFMPEG`·`FFPROBE`·`AUTOEDIT_FONTS`·`CHROME`)로 받는다.
- ⛔ **문서에 「대표」·「대표님」 같은 조직 호칭을 쓰지 않는다.** 읽는 사람 자리는 **「사용자」**다.
  받은 지시를 원문으로 인용하지 않는다 — 사실만 남기고 화자를 지운다.
- ⛔ **실측 수치에 회차 이름·날짜를 붙이지 않는다.** 「어떤 영상에서 재 본 예」로 쓰고,
  **재는 방법**을 함께 적는다. 남의 맥에서는 숫자가 다르게 나온다.

## 알아둘 함정

- **`ffmpeg` 는 반드시 `ffmpeg-full`.** 기본 ffmpeg 엔 libass·freetype 이 없어
  `subtitles`·`ass`·`drawtext` 필터가 0개다. **에러 없이 자막만 안 나온다.**
- **정지 PNG 오버레이에 `fade` 를 걸면 카드가 «에러 없이 안 보인다».**
  자세한 건 `auto-insert/references/traps.md`.
- **ffmpeg `drawtext` 는 텍스트의 `%` 를 서식으로 해석한다** → `expansion=none` 을 붙인다.
- **판서 감지는 고정 카메라에서만.** 짐벌·얼굴추적 카메라는 화면이 늘 변해 전 구간이 "판서 중"이 된다.
- **`ffmpeg -filters` 를 PATH 로 세면 제대로 깔고도 0 이 나온다.** `ffmpeg-full` 은 keg-only —
  `/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg` 전체 경로로 세야 한다.
- **파이썬은 `~/.autoedit/venv/bin/python`.** 시스템 `python3` 에는 numpy·fontTools 가 없다.
- **whisper 낱말 시각(`t_dtw`)은 `-nfa` 없이는 전부 −1 로 나온다.** 에러는 안 난다.
- 각 폴더 `SKILL.md` 와 `references/traps.md` 에 실제로 터졌던 사고가 표로 정리돼 있다. **먼저 읽을 것.**

## 구조

| 폴더 | 무엇 |
|---|---|
| `auto-multicam/` | 카메라 2대 → 싱크·판서 감지·전환 후보 → 🙋검토표 → 렌더 |
| `auto-insert/` | 자막·인서트컷·OTS 카드·방송형 오버레이 → 🙋검수 시트 → 렌더 |
| `auto-insert/scripts/mac/pipeline_v2/` | 화면공유+얼굴PIP 영상 전용 공정 |
| `auto-insert/scripts/mac/ots_v2/` | OTS 카드 5종 — 자리·가독성을 스스로 재는 부품 |
| `auto-insert/scripts/*.ps1` | 윈도우 원본 (맥에서 안 돎, 참고용) |
| `examples/` | 20초 샘플 — 고치기 전에 여기서 한 바퀴 돌려 확인한다 (10초면 OTS 카드 두 장이 최소 간격 8초에 걸린다) |

## 문서를 고칠 때

- 명령을 문서에 쓰기 전에 **직접 실행해 본다.** 안 되는 명령을 적는 것이 이 저장소에서 제일 흔한 사고였다.
- 모든 명령은 **저장소 루트에서** 도는 형태로 쓴다. `S=<이 저장소>` 같은 플레이스홀더는 쓰지 않는다
  (`<` 는 셸 리다이렉트라 복붙하면 조용히 빈 값이 된다).
- 입력 JSON 을 새로 만들거나 칸을 바꾸면 **`파일형식.md` 도 같이 고친다.**
