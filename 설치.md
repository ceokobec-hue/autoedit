# 설치 — 0단계부터 차례로

**이 문서는 맥(Apple Silicon) 기준입니다.**
윈도우를 쓰신다면 → [부록. 윈도우에서 설치하기](#부록-윈도우에서-설치하기) (⚠️아직 실기 확인 못 함)

터미널을 여세요. (Spotlight `⌘ + 스페이스` → "터미널" 입력 → 엔터)

**미리 필요한 것**

| 무엇 | 왜 | 없으면 |
|---|---|---|
| Homebrew | 맥에 프로그램을 받는 도구 | <https://brew.sh> |
| Google Chrome | 카드·인서트컷 그림을 **굽는 데 씁니다.** 화면에 안 띄우고 뒤에서 조용히 씁니다 | `brew install --cask google-chrome` |
| 파이썬 3.9 이상 | 맥에 이미 있습니다 | `python3 --version` 으로 확인 |

---

## 0단계. 저장소 받기 ⛔ 여기부터입니다

터미널을 열면 여러분은 지금 **집 폴더**에 서 있습니다. 도구 파일들이 아직 없으니
먼저 받아 오고, **그 폴더 안으로 들어가야** 합니다.

```bash
cd ~/Downloads
git clone https://github.com/ceokobec-hue/autoedit.git
cd autoedit
```

**★ 이 문서와 다른 문서의 모든 명령은 «이 `autoedit` 폴더 안에서» 칩니다.**
터미널을 새로 열 때마다 `cd ~/Downloads/autoedit` 를 먼저 치세요.
지금 어디에 서 있는지는 `pwd` 로 확인합니다 — 끝이 `/autoedit` 이면 맞습니다.

> **`git` 이 뭔가요?** 인터넷에 있는 폴더를 통째로 내 컴퓨터에 복사해 오는 도구입니다.
> 맥에는 대개 이미 들어 있습니다. `command not found: git` 이 뜨면 `xcode-select --install` 을
> 한 번 치고 창이 뜨면 「설치」를 누르세요.

**`git` 을 쓰기 싫으시면** — 브라우저로 <https://github.com/ceokobec-hue/autoedit> 에 가서
초록색 **Code** 버튼 → **Download ZIP** → 받은 파일을 두 번 눌러 압축을 풉니다.
풀린 `autoedit-main` 폴더를 **오른쪽 클릭 → 서비스 → 폴더에서 새로운 터미널 열기**.
그러면 그 폴더 안에서 터미널이 열립니다.

---

## 1단계. ffmpeg-full 깔기 ⛔ 제일 중요

```bash
brew install ffmpeg-full
```

**왜 `ffmpeg` 가 아니라 `ffmpeg-full` 인가요?**

영상 도구계의 만능 칼이 `ffmpeg` 입니다. 그런데 Homebrew 가 기본으로 주는 `ffmpeg` 에는
**글자를 그리는 부품(libass·freetype)이 빠져 있습니다.** 이걸 모르고 자막을 구우면
**에러 하나 없이 자막만 안 나옵니다.** 제일 골치 아픈 종류의 고장입니다.

확인:

```bash
/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg -filters | grep -cwE "subtitles|ass|drawtext"
```

**3** 이 나와야 합니다.

> ⚠️ **그냥 `ffmpeg -filters ...` 로 세면 제대로 깔고도 0 이 나옵니다.**
> `ffmpeg-full` 은 **keg-only** — 기존 `ffmpeg` 를 건드리지 않으려고 **PATH에 올라오지 않는**
> 방식으로 깔리기 때문입니다. 그래서 위처럼 **전체 경로를 적어** 확인해야 합니다.
> 인텔 맥이면 경로가 `/usr/local/opt/ffmpeg-full/bin/ffmpeg` 입니다.
> 다른 곳에 깔았으면 `export FFMPEG=/내/경로/ffmpeg` 로 알려 주세요.

**손으로 셀 필요 없이** 아래 3단계까지 끝낸 뒤 `python3 doctor.py` 하나로 확인해도 됩니다.
doctor 는 이 경로를 알아서 찾습니다.

### 자막(SRT)이 아직 없다면 — whisper 도 함께

이 도구들은 **자막 파일(SRT)이 이미 있다는 전제**로 시작합니다. 없으면 여기서 만듭니다.

```bash
brew install whisper-cpp
```

> ⚠️ `whisper-cli` 는 `ffmpeg-full` 에 딸려 오지 않습니다. **따로 받아야 합니다.**
> 쓰는 법은 [자막 만들기](#부록-자막srt와-문장-json-만들기) 를 보세요.

---

## 2단계. 파이썬 준비 ⛔ 마지막 줄이 제일 중요

맥에는 파이썬이 이미 있습니다. 부품 두 개만 더 받습니다.

```bash
python3 -m venv ~/.autoedit/venv
~/.autoedit/venv/bin/pip install fonttools numpy
```

- **fonttools** — 글자 폭을 실측합니다. 판 크기를 글자에 맞춰 정할 때 씁니다.
- **numpy** — 소리 파형과 화면 밝기를 «숫자 덩어리»로 빠르게 계산합니다.
  오토멀티캠(싱크·판서 감지)과 인서트 자리 계산이 이걸 씁니다.

> **가상환경(venv)이 뭔가요?** 이 도구 전용 **작은 방**을 하나 만드는 겁니다.
> 여기에 뭘 깔아도 맥 전체의 파이썬은 안 건드립니다. 지우고 싶으면
> `~/.autoedit/venv` 폴더만 지우면 흔적이 남지 않습니다.

### ★ 이제부터 `python3` 대신 이걸 씁니다

```bash
~/.autoedit/venv/bin/python
```

**왜냐면** — 부품을 «작은 방»에 넣었으니, **그 방의 파이썬을 불러야** 부품이 보입니다.
바깥의 `python3` 로 부르면 방 안이 안 보여서 `ModuleNotFoundError: No module named 'numpy'` 로 멈춥니다.

길어서 귀찮으니, **터미널을 열 때마다 한 줄** 정해 두고 그걸 씁니다.

```bash
PY=~/.autoedit/venv/bin/python
```

이러면 이 문서와 다른 문서들이 쓰는 `$PY ...` 형태를 그대로 복붙할 수 있습니다.

```bash
$PY doctor.py                    # ← 이렇게
```

> `PY=...` 는 터미널을 닫으면 사라집니다. 매번 치기 싫으면
> `~/.zshrc` 파일 맨 아래에 `export PY=~/.autoedit/venv/bin/python` 한 줄을 넣으세요.
> (`~/.zshrc` = 터미널이 열릴 때마다 자동으로 읽는 설정 파일)

---

## 3단계. 자막 폰트 받기

```bash
bash auto-insert/scripts/mac/get_fonts.sh
```

> 같은 일을 하는 파이썬판도 있습니다: `python3 auto-insert/scripts/mac/get_fonts.py`
> (윈도우엔 bash 가 없어서 만든 것인데, 맥에서도 똑같이 돕니다.)

`~/.autoedit/fonts` 에 받아집니다. 전부 **무료 · 상업 이용 가능** 폰트입니다.

| 무엇 | 어디에 쓰나 |
|---|---|
| 자막용 **OTF/TTF 8종** | ffmpeg 이 영상에 자막을 구울 때 (libass 는 웹폰트를 못 읽습니다) |
| 카드 렌더용 **woff2 4종** | Chrome 이 카드·인서트컷 HTML 을 그릴 때 |
| 각 폰트의 **라이선스 전문** | `~/.autoedit/fonts/licenses/` 에 함께 받아 둡니다 |

**자동으로 못 받는 2종**(배민 도현 · G마켓 산스)은 공식 페이지가 동의 절차를 거치게 돼 있어
직접 받으셔야 합니다. 스크립트가 끝날 때 받는 주소와 넣을 이름을 알려 줍니다.
**안 받아도 나머지는 전부 돕니다** — 예능형 강조 글꼴 후보가 둘 줄어들 뿐입니다.

> **왜 저장소에 폰트를 안 넣어 뒀나요?**
> 8종의 라이선스가 제각각이기 때문입니다. 넷은 SIL OFL(자유 재배포)이지만,
> 국내 기업 글꼴 넷은 **재배포하려면 각사의 라이선스 전문을 함께 넣어야** 하고,
> 배민 도현·G마켓 산스는 공식 페이지에서만 받게 돼 있습니다.
> 여덟 벌의 조건을 한 저장소에서 정확히 지키는 것보다,
> **각자 공식 배포처에서 받는 쪽이 안전하고 언제나 최신**이라 스크립트만 넣었습니다.
> 어떤 폰트가 어떤 라이선스인지는 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md) 에 정리해 뒀습니다.

**다른 곳에 두고 싶으면:**

```bash
export AUTOEDIT_FONTS=~/내폰트폴더
bash auto-insert/scripts/mac/get_fonts.sh
```

> ⚠️ `export` 로 정한 값은 **그 터미널 창에서만** 삽니다. 창을 닫으면 사라져서
> 다음에 `doctor.py` 가 다시 「폰트가 없습니다」라고 합니다.
> 계속 쓰시려면 `~/.zshrc` 맨 아래에 `export AUTOEDIT_FONTS=~/내폰트폴더` 한 줄을 넣으세요.

---

## 확인

```bash
python3 doctor.py
```

**전부 ✅ 면 준비 끝입니다.** 하나라도 ⛔ 면 그 줄이 알려 주는 단계로 돌아가세요.

---

## 그 다음엔 — 5분 안에 결과물 하나

설치가 됐는지는 **결과물로** 확인하는 게 제일 확실합니다. 샘플이 들어 있습니다.

```bash
PY=~/.autoedit/venv/bin/python
python3 examples/make_sample.py       # 시험용 영상 + 자막을 만듭니다 → examples/
$PY auto-insert/scripts/mac/srt_tools.py sync examples/sample.srt examples/sample.mp4
```

```
영상 00:20.0  ·  SRT 끝 00:20.0  ·  차이 0.0초
✅ 같은 컷편집본
```

이렇게 나오면 **여러분 맥에서 이 도구가 실제로 돈 겁니다.**
그다음은 `examples/README.md` 가 이어서 안내합니다.

그 뒤에 하고 싶은 걸 고르세요.

| 하고 싶은 것 | 볼 문서 |
|---|---|
| **처음이라면** | `examples/README.md` — 샘플로 한 바퀴 |
| 카메라 두 대 붙이기 | `auto-multicam/SKILL.md` |
| 자막·인서트컷 얹기 | `auto-insert/scripts/mac/README.md` |
| 화면공유 영상에 카드 얹기 | `auto-insert/scripts/mac/pipeline_v2/README.md` |
| OTS 카드 | `auto-insert/scripts/mac/ots_v2/README.md` |
| **JSON 파일에 뭘 적어야 하나** | [`파일형식.md`](파일형식.md) — 입력 파일 모양 한눈에 |

---

## 부록. 자막(SRT)과 문장 JSON 만들기

두 도구 다 **자막이 있어야 시작**합니다. 이미 SRT 가 있으면 이 부록은 건너뛰세요.

### ① 소리만 뽑기

whisper 는 **16kHz 모노 wav** 만 받습니다. 오토멀티캠의 싱크 측정도 같은 형식을 씁니다.

```bash
python3 examples/make_wav.py 내영상.mp4 내영상.wav
```

> ⛔ 전에는 여기에 `FF=/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg` 처럼 **경로를 통째로 적어** 뒀는데,
> 그 경로는 인텔 맥·윈도우·다른 곳에 받으신 분께 **전부 틀립니다.**
> `make_wav.py` 는 `ff_path.py` 가 찾은 ffmpeg 을 씁니다. 윈도우도 같은 한 줄입니다
> (`python examples\make_wav.py 내영상.mp4 내영상.wav`).

이 한 줄이 하는 일:

- `-vn` 그림 버리기 · `-ac 1` 한 채널(모노) · `-ar 16000` 초당 16000번 표본
- `-c:a pcm_s16le` 압축 안 한 16비트 소리

### ② 모델 한 번 받기

```bash
curl -L -o ~/.autoedit/ggml-small.bin \
  https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-small.bin
```

작을수록 빠르고 부정확합니다. **한국어는 `small` 이상**을 권합니다
(`base` 는 「취합」을 「치압」으로 듣습니다). `small` 은 약 470MB, 한 번만 받으면 됩니다.

### ③ 받아쓰기

```bash
whisper-cli -m ~/.autoedit/ggml-small.bin -f 내영상.wav -l ko -osrt -of 내영상
# → 내영상.srt
```

### ④ (오토멀티캠만) 문장 JSON

오토멀티캠은 자막 큐가 아니라 **낱말 하나하나의 시각**으로 문장을 다시 잘라냅니다.
그래야 문장 한가운데서 카메라가 바뀌지 않습니다. 그러려면 옵션 셋이 더 필요합니다.

```bash
whisper-cli -m ~/.autoedit/ggml-small.bin -f 내영상.wav -l ko \
  -ojf -dtw small -nfa -of 내영상
# → 내영상.json
```

| 옵션 | 뜻 |
|---|---|
| `-ojf` | 자세한 JSON 으로 내보내기 |
| `-dtw small` | **낱말 단위 시각**을 계산 (뒤의 이름은 쓰는 모델과 같게) |
| `-nfa` | ⛔ **이게 없으면 낱말 시각이 전부 −1 로 비어서 나옵니다** |

> **`-nfa` 를 왜 꼭 붙여야 하나요?** whisper 가 기본으로 켜는 「flash attention」이라는 빠른 계산
> 방식과 낱말 단위 시각이 **같이 못 씁니다.** whisper 는 이때 에러를 내지 않고
> `dtw_token_timestamps is not supported with flash_attn - disabling` 한 줄만 찍고 **조용히 꺼 버립니다.**
> 그 JSON 을 그대로 쓰면 문장이 엉뚱하게 잘립니다. 이 저장소가 제일 경계하는
> 「에러 없이 틀린 결과」의 전형입니다.

제대로 나왔는지 한 줄로 확인:

```bash
python3 -c "import json,sys; d=json.load(open('내영상.json')); \
t=[x for s in d['transcription'] for x in s['tokens']]; \
print('낱말', len(t), '· 시각 있는 것', sum(1 for x in t if x.get('t_dtw',-1)>=0))"
```

「시각 있는 것」이 0 이면 `-nfa` 를 빠뜨린 겁니다.

---

## 부록. 윈도우에서 설치하기

> ⚠️ **아직 윈도우 실기에서 확인하지 못했습니다.**
> 윈도우에서 걸릴 자리를 코드에서 치워 두기는 했지만, 「된다」고 확인한 것이 아닙니다.
> 막히시면 [Issues](https://github.com/ceokobec-hue/autoedit/issues) 에
> **`python doctor.py` 결과를 그대로** 붙여 주세요.

아래는 **명령 프롬프트(cmd)** 기준입니다.
PowerShell 을 쓰신다면 `%USERPROFILE%` → `$env:USERPROFILE`, `set A=B` → `$env:A="B"` 로 바꿔 주세요.

### 0단계. 저장소 받기

```
git clone https://github.com/ceokobec-hue/autoedit.git
cd autoedit
```

`git` 이 없으면 <https://git-scm.com> 에서 받거나, GitHub 페이지에서 **Code → Download ZIP** 으로
받아 압축을 푸신 뒤 그 폴더에서 명령 프롬프트를 여세요.
**이 문서의 모든 명령은 그 `autoedit` 폴더 안에서** 칩니다.

### 1단계. ffmpeg 받기 ⛔ 제일 중요

윈도우엔 Homebrew 가 없어 **직접 받으셔야 합니다.** 공식 안내:
<https://ffmpeg.org/download.html#build-windows>

> ⛔ **아무 빌드나 받으면 안 됩니다. 자막 필터(libass)가 든 빌드**여야 합니다.
> 없는 빌드로 굽으면 **에러 하나 없이 자막만 안 나옵니다.**
> 맥에서 `ffmpeg` 대신 `ffmpeg-full` 을 받으라고 하는 이유와 **똑같은 이유**입니다.
>
> `winget install Gyan.FFmpeg` 처럼 한 줄로 받는 방법도 돌아다니지만,
> **저는 확인해 보지 못했습니다.** 확인 못 한 명령을 이 문서에 적지 않겠습니다.
> 어느 쪽으로 받으시든 **판정은 `doctor.py` 가** 합니다 — 「자막 필터 3/3」이면 맞는 빌드입니다.

받은 폴더 안 `bin` 을 알려 주세요 (그 안에 `ffmpeg.exe` 와 `ffprobe.exe` 가 있습니다).

```
set FFMPEG_BIN=C:\받은곳\bin
```

> ⚠️ `set` 으로 정한 값은 **그 창에서만** 삽니다. 창을 닫으면 사라집니다.
> 계속 쓰시려면 「시스템 환경 변수 편집」에 넣으세요.

### 2단계. 파이썬 준비

```
python -m venv %USERPROFILE%\.autoedit\venv
%USERPROFILE%\.autoedit\venv\Scripts\pip install fonttools numpy
```

> ⚠️ `python` 을 쳤는데 **마이크로소프트 스토어가 열리면** 파이썬이 없는 것입니다.
> <https://www.python.org/downloads/> 에서 받으시거나, 이미 받으셨다면 `py` 로 바꿔 쳐 보세요.

⛔ **윈도우 venv 는 `bin` 이 아니라 `Scripts` 입니다.** 이제부터 `python` 대신 이걸 씁니다:

```
%USERPROFILE%\.autoedit\venv\Scripts\python
```

### 3단계. 폰트 받기

```
python auto-insert\scripts\mac\get_fonts.py
```

> 맥용 `get_fonts.sh` 와 하는 일이 **같습니다.** 윈도우엔 bash·curl·unzip 이 없어
> 파이썬 표준 부품(urllib·zipfile)만으로 다시 쓴 것입니다. 따로 받을 것이 없습니다.
> 폴더 이름이 `scripts\mac\` 인 건 옛 이름이 남은 것뿐입니다 — 윈도우에서도 이 파일을 씁니다.

내려받다 실패하면 **어느 주소에서 무엇을 받아 어떤 이름으로 넣으면 되는지** 끝에 알려 줍니다.

### 확인

```
python doctor.py
```

맥과 다르게 나오는 줄이 있습니다.

| 줄 | 윈도우에서는 |
|---|---|
| **영상 인코더** | NVIDIA 그래픽카드가 **실제로 굽는지 시험해 보고** `h264_nvenc` 또는 `libx264` 로 나옵니다 |
| **자막 필터 3/3** | 여기서 3 이 아니면 **1단계로 돌아가** 다른 빌드를 받으세요 |
| **크롬** | 크롬이 없으면 윈도우에 이미 있는 **Microsoft Edge** 를 찾아 씁니다 (같은 엔진) |
| **Apple Vision** | ⚠️ 로 나옵니다. 「인물 옆 자막」·OTS 자리 판정이 맥에 내장된 얼굴 인식을 쓰기 때문입니다 |

### 첫 결과물

```
python examples\make_sample.py
%USERPROFILE%\.autoedit\venv\Scripts\python auto-insert\scripts\mac\srt_tools.py sync examples\sample.srt examples\sample.mp4
```

### 환경변수 한눈에

| 이름 | 무엇 | 예 |
|---|---|---|
| `FFMPEG_BIN` | ffmpeg 을 둔 **폴더** | `C:\ffmpeg\bin` |
| `FFMPEG` · `FFPROBE` | 실행파일을 **하나씩** 지정 | `C:\ffmpeg\bin\ffmpeg.exe` |
| `CHROME` | 크롬(또는 엣지) 실행파일 | `C:\...\chrome.exe` |
| `AUTOEDIT_FONTS` | 폰트 창고 폴더 | 기본 `%USERPROFILE%\.autoedit\fonts` |
| `AUTOEDIT_ENCODER` | 인코더 **못박기** | `libx264` · `h264_nvenc` |
| `AUTOEDIT_CRF` | `libx264` 화질 눈금 | 기본 `20` · 작을수록 고화질 |

> **인코더를 왜 못박나요?** 자동 판정은 **그때그때 다시 잽니다.** 다른 프로그램이 그래픽카드
> 인코더를 다 쓰고 있으면 그 순간엔 `libx264` 로 내려갑니다(결과는 정상, 느려질 뿐).
> 며칠에 걸쳐 나눠 렌더하면서 **매번 똑같은 조건으로 굽고 싶다면** `AUTOEDIT_ENCODER` 로 못박으세요.

### 윈도우에서 아직 안 되는 것

| | |
|---|---|
| ⛔ 인물 옆 자막 · OTS 자리 판정 | 맥에 내장된 얼굴 인식(Apple Vision)을 씁니다 |
| ⚠️ 한글 경로 | 작업 폴더는 영문 경로가 안전합니다 (맥에서도 같습니다) |
| ⚠️ `auto-insert/scripts/*.ps1` | 이 파이썬판이 나오기 전의 **옛 물건**입니다. 별개이고, 이번에 손대지 않았습니다 |

---

## 잘 안 될 때

| 증상 | 원인 · 할 일 |
|---|---|
| `No such file or directory: get_fonts.sh` · `doctor.py` | **0단계.** 저장소 폴더 밖에 서 있습니다. `cd ~/Downloads/autoedit` |
| 자막이 **에러 없이** 안 나온다 | **1단계.** `/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg -filters \| grep -cwE "subtitles\|ass\|drawtext"` 가 3인지 |
| `ffmpeg -filters` 가 0 이다 | 정상입니다. `ffmpeg-full` 은 PATH 에 안 올라옵니다. **전체 경로로** 다시 세세요 |
| `ModuleNotFoundError: No module named 'numpy'` (또는 `fontTools`) | **2단계.** `python3` 말고 `~/.autoedit/venv/bin/python` 으로 부르세요 |
| `⛔ 폰트가 없습니다` | **3단계.** 새 터미널이면 `export AUTOEDIT_FONTS=...` 가 사라졌을 수도 있습니다 |
| `command not found: brew` | Homebrew 부터 (<https://brew.sh>) |
| `command not found: whisper-cli` | `brew install whisper-cpp` |
| 낱말 시각(`t_dtw`)이 전부 −1 | whisper 명령에 **`-nfa`** 를 빠뜨렸습니다 |
| 카드·인서트 PNG 가 안 만들어진다 | Chrome 이 없거나 다른 곳에 있습니다. `brew install --cask google-chrome` |
| 글자가 네모(□)로 나온다 | 그 폰트에 그 글자가 없습니다. `$PY auto-insert/scripts/mac/srt_tools.py fontcheck 폰트파일 --srt 자막.srt` |
| 한글 파일명에서 터진다 | 작업 폴더는 영문 경로를 쓰고, 완성본만 한글 폴더로 옮기세요 |
| **(윈도우)** `bash: command not found` | 윈도우엔 bash 가 없습니다. `.sh` 대신 **파이썬판**을 쓰세요 — `get_fonts.py` · `make_sample.py` |
| **(윈도우)** `python` 을 쳤더니 스토어가 열린다 | 파이썬이 없는 것입니다. <https://www.python.org/downloads/> · 이미 받았으면 `py` 로 |
| **(윈도우)** 「작은 방(venv)이 없습니다」가 계속 뜬다 | 윈도우 venv 는 `bin` 이 아니라 **`Scripts`** 입니다. `%USERPROFILE%\.autoedit\venv\Scripts\python` |
| **(윈도우)** 렌더가 너무 느리다 | 그래픽카드 인코더를 못 써서 `libx264` 로 굽는 중입니다. `python doctor.py` 의 「영상 인코더」 줄을 보세요 |
| **(윈도우)** 자막이 **에러 없이** 안 나온다 | 받으신 ffmpeg 빌드에 자막 필터가 없습니다. **1단계**로 돌아가 다른 빌드를 받으세요 |
