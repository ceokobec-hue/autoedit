# 입력 JSON — 어디에 뭘 적나

이 도구들은 **여러분이 손으로 쓰는 JSON 몇 개**로 움직입니다.
「어느 파일을 누가 만들고, 누가 읽고, 안에 뭘 적어야 하는지」를 한 장에 모았습니다.

> **JSON 이 뭔가요?** 컴퓨터가 읽는 «양식 종이»입니다.
> `{ }` 안에 `"칸이름": 값` 을 쉼표로 나열하고, `[ ]` 는 여러 개를 줄 세운 목록입니다.
> 한글도 그대로 씁니다. 다만 **맨 마지막 항목 뒤에 쉼표를 찍으면 안 됩니다** — 제일 흔한 실수입니다.
>
> 잘 썼는지 확인은 한 줄로:
> ```bash
> python3 -m json.tool 내파일.json > /dev/null && echo "형식 OK"
> ```

**실제로 도는 예시 파일들이 `examples/` 에 들어 있습니다.** 복사해서 고쳐 쓰는 게 제일 빠릅니다.

---

## 한눈에 — 누가 만들고 누가 읽나

| 파일 | 누가 만드나 | 누가 읽나 |
|---|---|---|
| `cards.json` | **사람** (카드 원고) | `ots_place.py` · `build_inserts.py` |
| `chapters.json` | **사람** (단원 소제목) | `broadcast_overlay.py` · `review_sheet.py` |
| `job.json` | **사람** (화면공유 영상 제원) | pipeline_v2 전부 |
| `plan.json` | **사람** (합성 계획) | `compose_mac.py` · `plan_check.py` |
| `variety.json` | **사람** (예능형 자막) | `make_ass_deco.py` |
| `ots_measure.json` | `ots_place.py` | `build_inserts.py --from-measure` |
| `place.json` | `build_inserts.py --from-measure` (또는 사람) | `build_inserts.py` |
| `inserts.json` | `build_inserts.py` | `review_sheet.py` · `render_final.py` |
| `plan_out.json` | `plan_cards.py` | `approve.py` |
| `check_cards.json` · `plan_check.json` · `plan_final.json` | `approve.py` | `build_sheet.py` · `render_ots_v2.py` |
| `sync.json` | `sync_probe.py` | `build_review.py` |
| `board_segs.json` | `analyze_bg.py` | `make_plan.py` · `shot_plan.py` |
| `sentences.json` | `sentences.py` | `shot_plan.py` |
| `cands.json` | `make_plan.py` / `shot_plan.py` | `build_review.py` · `render_multicam.py` |
| `switch_plan.json` | `render_multicam.py` | `qa_sheet.py` |

**필수** 표시가 없는 칸은 빼도 됩니다.

---

# 사람이 쓰는 것

## 1. `cards.json` — 카드 원고 (방송형 오버레이)

화면에 띄울 카드 내용입니다. **목록(`[ ]`)** 으로 씁니다.

```json
[
 {"id":"C1","kind":"corner","at":34.6,"dur":6.0,"label":"첫 카드",
  "kicker":"작은 라벨","head":"큰 문구<br>두 줄까지",
  "rows":[{"n":"1","t":"항목 이름","v":"값","unit":"개"}],
  "foot":"아래 한 줄","src":"출처"},

 {"id":"F1","kind":"full","at":120.0,"dur":7.0,"label":"풀프레임 카드",
  "kicker":"라벨","head":"화면 전체를 덮는 카드","desc":"한 줄 보충"}
]
```

| 칸 | 타입 | 필수 | 무엇 |
|---|---|---|---|
| `id` | 문자 | ✅ | 카드 이름표. **겹치면 안 됩니다.** 결과 PNG 파일명이 됩니다 |
| `kind` | `"corner"` / `"full"` | ✅ | 어깨 옆 작은 카드 / 화면을 통째로 덮는 카드 |
| `at` | 숫자(초) | ✅ | 카드가 뜨는 시각. `34.6` = 34.6초 |
| `dur` | 숫자(초) | ✅ | 몇 초 떠 있나. 보통 5~8 |

> ⛔ **`at` 과 `dur` 은 카드마다 반드시 있어야 합니다.** 빠지면 한국어로 알려 주고 멈춥니다.
> 「몇 초에, 몇 초 동안」이 없으면 자리를 잴 프레임을 고를 수가 없기 때문입니다.
| `label` | 문자 | | 검수 시트에서 알아보려고 붙이는 이름. **없으면 `head`→`keyword`→`kicker`→`h1`→`id` 순으로 알아서 씁니다** |
| `kicker` | 문자 | ✅ | 맨 위 작은 라벨 |
| `head` | 문자 | ✅ | 큰 문구. 줄을 바꾸려면 `<br>`. **8자 이내 × 2줄**까지 |
| `rows` | 목록 | | 항목 나열. `t`=제목 · `v`=값 · `unit`=값 뒤 단위 · `n`=앞 번호 |
| `big` / `bigunit` | 문자 | | 아주 큰 숫자 하나를 보여줄 때 |
| `desc` | 문자 | | 한 줄 보충 설명 |
| `foot` / `src` | 문자 | | 카드 맨 아래 왼쪽 / 오른쪽 |

## 2. `chapters.json` — 단원 소제목

```json
[
 {"at": 0.0,   "title": "여는 말"},
 {"at": 145.5, "title": "첫 번째 단원"},
 {"at": 620.0, "title": "두 번째 단원"}
]
```

| 칸 | 타입 | 필수 | 무엇 |
|---|---|---|---|
| `at` | 숫자(초) | ✅ | 이 소제목으로 바뀌는 시각 |
| `title` | 문자 | ✅ | 화면 좌상단에 뜰 글자 |

> ⚠️ **단원 단위로만** 넣습니다. 20분에 9개쯤이 적당합니다. 촘촘하면 산만합니다.

## 3. `job.json` — 화면공유+얼굴PIP 영상 제원 (pipeline_v2)

```json
{
 "video":   "/절대경로/편집끝난_영상.mp4",
 "srt":     "/절대경로/자막-한국어.srt",
 "workdir": "/절대경로/작업폴더",
 "out":     "완성본.mp4",
 "width": 2560, "height": 1440,
 "sub_band": [1190, 1345],
 "pip":      [1795, 0, 2560, 435],
 "bug_zone": [0, 0, 600, 210]
}
```

| 칸 | 타입 | 필수 | 무엇 |
|---|---|---|---|
| `video` `srt` `workdir` | 문자 | ✅ | **전부 `/` 로 시작하는 절대경로**로 씁니다 |
| `out` | 문자 | | 완성본 이름 (기본 `완성본.mp4`) |
| `width` `height` | 숫자 | ✅ | 영상 해상도. 좌표 계산의 기준입니다 |
| `sub_band` | `[위, 아래]` | ✅ | **이미 구워진 자막**이 차지한 세로 띠. 여기는 안 덮습니다 |
| `pip` | `[왼, 위, 오른, 아래]` | ✅ | 얼굴 작은 화면 자리 |
| `bug_zone` | `[왼, 위, 오른, 아래]` | ✅ | 채널 버그(로고 판) 자리 |

`measure_layout.py` · `measure_pip.py` 로 이 좌표들을 **재서** 정합니다. 눈대중으로 넣지 마세요.

## 4. `plan.json` — 합성 계획 (`compose_mac.py`)

인서트 mp4 들을 원본 위에 덮고, 자막을 굽고, 범퍼를 끼우는 3단계 계획서입니다.

```json
{
 "src":  "/절대경로/편집원본.mp4",
 "work": "/절대경로/작업폴더",
 "out":  "/절대경로/완성본.mp4",
 "ass":  "deco.ass",
 "fontsdir": "/Users/나/.autoedit/fonts",
 "inserts_dir": "/절대경로/인서트mp4들",
 "inserts": [ {"id":"INS-01","ts":12.4,"d":5.0} ],
 "bumpers_dir": "/절대경로/범퍼mp4들",
 "bumpers_over": [ {"file":"BMP-01.mp4","ts":300.0,"d":4.0} ],
 "bumpers_ins":  [ {"file":"BMP-15.mp4","ts":700.0,"d":4.0} ]
}
```

| 칸 | 타입 | 필수 | 무엇 |
|---|---|---|---|
| `src` | 문자 | ✅ | 편집 끝난 원본 |
| `work` | 문자 | ✅ | 중간 파일이 쌓일 폴더 |
| `out` | 문자 | | 완성본 (기본 `work/완성본.mp4`) |
| `ass` | 문자 | | 구울 자막 파일 (`make_ass_deco.py` 산출) |
| `fontsdir` | 문자 | | 폰트 폴더 (기본 `~/.autoedit/fonts`) |
| `inserts_dir` + `inserts[]` | | | **덮어쓰기** 인서트. `id`+`.mp4` 로 파일을 찾습니다. `ts`=시작초 `d`=길이초 |
| `bumpers_over[]` | | | **덮어쓰기** 범퍼 — 길이가 안 늘어납니다 |
| `bumpers_ins[]` | | | **삽입** 범퍼 — 여기서만 영상이 길어집니다 |

> ⛔ **순서가 규약입니다** — 1 인서트 덮어쓰기 → 2 자막 굽기 → 3 범퍼 삽입.
> 범퍼가 시간을 밀어내므로, 자막을 나중에 구우면 자막 수백 개의 시각을 전부 다시 계산해야 합니다.

## 5. `variety.json` — 예능형 자막

대사를 옮기는 게 아니라 **리액션 한마디**를 화면에 던지는 자막입니다.

```json
[
 {"s": 42.0, "e": 44.2, "t": "여기 중요!", "size": 104, "x": 1280, "y": 180, "color": "#FFE24B"}
]
```

| 칸 | 타입 | 필수 | 무엇 |
|---|---|---|---|
| `s` `e` | 숫자(초) | ✅ | 뜨는 시각 · 사라지는 시각 |
| `t` | 문자 | ✅ | 화면에 뜰 글자 |
| `size` | 숫자 | | 글자 크기 (기본 104) |
| `x` `y` | 숫자 | | 화면 좌표. 기본은 가로 가운데 · 위에서 180 |
| `color` | `"#RRGGBB"` | | 글자색 |

## 6. `cards.json` (ots_v2 용) — 카드 계획

같은 이름이지만 **ots_v2 는 칸이 다릅니다.** 헷갈릴 수 있는데, **잘못 넣으면 스크립트가
「어느 파이프라인으로 가라」고 알려주고 멈춥니다.** 조용히 틀린 결과가 나오지는 않습니다.

**어느 쪽인지는 `kind` 로 구분합니다.**

| `kind` | 어느 파이프라인 | 큰 글씨 칸 |
|---|---|---|
| `corner` · `full` | **방송형 오버레이** (`ots_place.py` → `build_inserts.py`) | `head` |
| `sticker` · `bento` · `rail` · `daepan` · `terminal` | **ots_v2** (`plan_cards.py` → `approve.py` → `cards_v2.py`) | `head` (단 `daepan` 은 `keyword`) |

```json
[
 {"id":"S1","kind":"sticker","at":34.6,"dur":5.5,
  "kicker":"작은 라벨","head":"큰 문구<br>두 줄까지","tag":"뱃지","emoji":"⚠️","emoji2":"💸",
  "rows":[{"t":"항목","v":"값","unit":"단위"}],
  "foot":"아래 한 줄","src":"출처"},

 {"id":"D1","kind":"daepan","at":111.3,"dur":6,
  "kicker":"작은 라벨","keyword":"큰 키워드","hi":"큰"}
]
```

| 칸 | 타입 | 필수 | 무엇 |
|---|---|---|---|
| `id` | 문자 | ✅ | 이름표 (겹치면 안 됨) |
| `kind` | `sticker`/`bento`/`rail`/`daepan`/`terminal` | ✅ | 카드 5종 → `ots_v2/README.md` |
| `at` `dur` | 숫자(초) | ✅ | 뜨는 시각 · 떠 있는 초 |
| `kicker` | 문자 | ✅ | 맨 위 작은 라벨 |
| `head` | 문자 | `daepan` 빼고 ✅ | 큰 문구. `<br>` 로 줄바꿈 |
| `keyword` | 문자 | `daepan` 만 ✅ | 판 없이 띄우는 큰 키워드. **글자 수로 크기가 자동** |
| `hi` | 문자 | | `keyword` 안에서 빨갛게 강조할 부분. **줄바꿈으로 찢기지 않습니다** |
| `rows` `foot` `src` `tag` `emoji` `emoji2` | | | 카드 종류에 따라 쓰입니다 |

---

# 기계가 만드는 것 (열어 보고 고칠 수도 있습니다)

## `ots_measure.json` — 자리 실측 결과 (`ots_place.py`)

```json
{"W":2560,"H":1440,"guard":1152,
 "cards":[{"id":"C1","at":34.6,"dur":6.0,"label":"첫 카드",
           "person_L":980,"person_R":1720,"left_free":980,"right_free":840,
           "slide_L":null,"slide_R":null,"n_person":5,"n_text":0}]}
```

- `guard` = **이미 구워진 자막의 윗변**. 카드는 이 선 위까지만 놓습니다
- `left_free` / `right_free` = 사람 왼쪽·오른쪽에 남은 가로 여백(px) → **넓은 쪽에 카드를 놓습니다**
- `person_L/R` 이 `null` 이면 그 구간에서 사람을 못 찾은 것입니다

## `place.json` — 카드별 자리 (`build_inserts.py --from-measure` 가 만듦)

```json
{"C1": {"w": 780, "h": 590, "corner": "좌하", "at": 34.6}}
```

| 칸 | 값 | 무엇 |
|---|---|---|
| `corner` | **`좌상` · `우상` · `좌하` · `우하`** | 카드를 붙일 모서리. 영어 `tl`/`tr`/`bl`/`br` 로 써도 받아 줍니다(한글로 바꿔 저장됩니다) |
| `w` `h` | 숫자(px) | 카드 크기 |
| `at` `dur` | 숫자(초) | 뜨는 시각 · 떠 있는 초 |

**자동 계산이 마음에 안 들면 이 파일을 손으로 고치면 됩니다.** 고친 뒤에는 `--from-measure` 없이
`--place place.json` 으로 다시 돌리세요 — 안 그러면 다시 계산해 덮어씁니다.

## `inserts.json` — 구운 카드 목록 (`build_inserts.py`)

`cards.json` 의 칸에 아래가 더 붙습니다.

| 칸 | 무엇 |
|---|---|
| `render` | `"corner"` 또는 `"full"` |
| `png` | 만들어진 그림 파일 경로 |
| `w` `h` `corner` `at` | 실제로 놓인 크기·모서리·시각 |
| `ok` | **`false` 면 그 카드는 안 만들어진 것입니다.** 그냥 넘어가지 말 것 |

## `sync.json` — 두 카메라의 밀림 (`sync_probe.py`)

| 칸 | 무엇 |
|---|---|
| `intercept` | **시작 밀림(초).** 이 값이 뒤 단계의 `--offset` 입니다 |
| `slope` · `speed` | 드리프트(시계 차이). `spread` 가 0.033 미만이면 무시해도 됩니다 |
| `probes[]` | 지점별 실측. `score`(일치도)가 낮은 지점이 많으면 결과를 믿지 마세요 |
| `durA` `durB` | 두 소리 파일 길이 |

## `board_segs.json` — 판서 구간 (`analyze_bg.py`)

```json
{"step":4,"t":[...],"ink":[...],"ink_max":12.3,
 "segs":[{"kind":"판서","start":120.0,"end":168.0,"gain":0.42,"i0":30,"i1":42}]}
```

`kind` 는 `판서`(글씨가 늘어남) 또는 `지움`. `gain` 은 잉크가 얼마나 늘거나 줄었는지입니다.

## `cands.json` — 전환 후보 (`make_plan.py` · `shot_plan.py`)

**검토표와 렌더가 둘 다 읽는, 오토멀티캠에서 제일 중요한 파일**입니다.

```json
{"dur": 1620.5,
 "anchors": {"board":[740,390], "center":[960,540], "face":[960,520]},
 "items":[
   {"no":1,"kind":"판서","cam":2,"start":120.0,"end":168.0,"dur":48.0,
    "zoom":135,"anchor":"board","keep":true,"why":"판서 구간","i0":30,"i1":42}
 ]}
```

| 칸 | 무엇 |
|---|---|
| `no` | 후보 번호. **검토표와 `--decide` 가 이 번호로 이야기합니다** |
| `cam` | 1 = 첫째 카메라(`--camA`) · 2 = 둘째(`--camB`) |
| `start` `end` | 이 후보가 걸치는 구간(초) |
| `zoom` | 100 = 원본 크기 · 120/135/150 = 확대 |
| `anchor` | 확대할 때 화면의 어디를 남길지. `anchors` 의 이름 중 하나 |
| `keep` | `true` 면 **기본으로 켜짐**. `--decide` 를 안 주면 이것만 씁니다 |
| `why` | 왜 이 후보가 나왔는지 (검토표에 그대로 보입니다) |
| `i0` `i1` | 「추가된 글씨」 그림을 그릴 때 쓰는 맨보드 번호. 판서 후보에만 있습니다 |

## `switch_plan.json` — 확정된 구간표 (`render_multicam.py`)

렌더가 실제로 만든 구간 목록입니다. `qa_sheet.py` 가 이걸 읽어 전환 지점을 뽑습니다.

```json
[{"start":0.0,"end":120.0,"cam":1,"zoom":100,"anchor":"face"}]
```

---

## 자주 나는 실수 셋

| 증상 | 원인 |
|---|---|
| `json.decoder.JSONDecodeError` | 마지막 항목 뒤에 **쉼표**를 찍었거나, 따옴표가 짝이 안 맞습니다 |
| 카드가 화면에 안 보인다 | `inserts.json` 의 `ok` 가 `false` 입니다. PNG 가 안 구워진 것 |
| 카드가 자막을 덮는다 | `guard`(자막 윗변)를 안 지킨 것. `ots_place.py` 를 다시 돌리세요 |
